# Idle actions are disabled — no dimming, lock, or suspend.
# Uncomment to re-enable. Timings below are the desktop-corrected
# versions (DPMS off rather than backlight dimming).

#general {
#    lock_cmd = pidof hyprlock || hyprlock || hyprlock -c ~/.local/src/Ghost/src/config/hyprlock.conf       # avoid starting multiple hyprlock instances.
#    before_sleep_cmd = loginctl lock-session    # lock before suspend.
#    #after_sleep_cmd = hyprctl dispatch 'hl.dsp.dpms({ action = "enable" })'  # to avoid having to press a key twice to turn on the display.
#}
#
#listener {
#    timeout = 330                                 # 5.5min
#    on-timeout = loginctl lock-session            # lock screen when timeout has passed
#}
#
#listener {
#    timeout = 360                                     # 6min
#    on-timeout = hyprctl dispatch dpms off            # displays off when timeout has passed
#    on-resume = hyprctl dispatch dpms on              # displays on when activity is detected.
#}
#
#listener {
#    timeout = 900                                # 15min
#    on-timeout = systemctl suspend                # suspend pc
#}
